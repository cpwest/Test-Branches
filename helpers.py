# Inside of `helpers.py`, write a function, called `greet`, 
# that accepts a name and prints: `"Hello, <name>"`. 
# For example, `greet("Jane")` would print: `"Hello, Jane"`.

def greet():
    name = input("What's your name?")
    print("Hello" + str(name) +"!")